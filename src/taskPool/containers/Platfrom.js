import React, { } from 'react';
import { Button, Table } from 'antd';

const Platform = (props) => {
  return <div>
    <h2>合作平台管理</h2>
    <Button type="primary">添加合作平台</Button>
    {/* <Table columns={} dataSource={[]} /> */}
  </div>
}
export default Platform
